{{!-- Aviso de mensagem ainda não lida no protocolo. Recebe: mostrar e texto. --}}
{{#if mostrar}}
    <span class="badge bg-danger rounded-pill px-3 py-2 novidade-pulsa">
        <i class="bi bi-bell-fill me-1"></i>{{texto}}
    </span>
{{/if}}
