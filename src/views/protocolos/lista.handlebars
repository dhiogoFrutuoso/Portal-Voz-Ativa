<header class="py-5 text-white shadow-lg" style="background: linear-gradient(145deg, #13315c 0%, #0056b3 100%); border-bottom: 5px solid #ffc107;">
    <div class="container px-4">
        <span class="badge bg-warning text-dark px-3 py-2 rounded-pill fw-bold mb-3">
            <i class="bi bi-folder-check me-1"></i> ACOMPANHAMENTO
        </span>
        <h1 class="display-5 fw-bolder mb-2">Meus Protocolos</h1>
        <p class="lead mb-0 opacity-75 fw-light">
            Acompanhe o andamento das melhorias e denúncias que você registrou.
        </p>
    </div>
</header>

<section class="py-5 bg-light">
    <div class="container px-4">

        {{!-- Estágios, busca e categoria ficam sob o mesmo painel de filtragem
              instantânea: é o que faz os botões realmente filtrar a lista
              abaixo, sem recarregar a página. --}}
        <div data-busca-painel data-busca-rota="/protocolos/buscar" data-busca-alvo="#resultados">
            <div class="row g-3 mb-4 filtros-com-selecao">
                <div class="col-6 col-lg">
                    <button type="button" class="card-estagio p-3 h-100 shadow-sm ativo" data-filtro="status" data-valor="">
                        <div class="text-dark fs-4 mb-1"><i class="bi bi-collection-fill"></i></div>
                        <div class="h4 fw-bold mb-0">{{total}}</div>
                        <div class="small text-muted">Todos</div>
                    </button>
                </div>

                {{#each resumo}}
                    <div class="col-6 col-lg">
                        <button type="button" class="card-estagio p-3 h-100 shadow-sm border-{{this.cor}}"
                                data-filtro="status" data-valor="{{this.chave}}">
                            <div class="text-{{this.cor}} fs-4 mb-1"><i class="bi {{this.icone}}"></i></div>
                            <div class="h4 fw-bold mb-0">{{this.total}}</div>
                            <div class="small text-muted">{{this.rotulo}}</div>
                        </button>
                    </div>
                {{/each}}
            </div>

            <div class="card border-0 shadow-sm rounded-4 mb-4">
                <div class="card-body p-3 p-md-4">
                    <form action="/protocolos" method="GET" class="row g-2 align-items-center">
                        <div class="col-12 col-md">
                            <div class="input-group">
                                <span class="input-group-text bg-white border-end-0"><i class="bi bi-search text-muted"></i></span>
                                <input type="text" name="q" value="{{termo}}" maxlength="100" autocomplete="off"
                                       class="form-control border-start-0 rounded-end-3" data-busca-campo
                                       placeholder="Buscar nos seus protocolos por título, descrição ou local...">
                            </div>
                        </div>
                        <div class="col-md-auto d-grid">
                            <button type="submit" class="btn btn-dark rounded-3 px-4 fw-bold">Buscar</button>
                        </div>
                    </form>

                    <div class="filtros-grupo mt-3">
                        <span class="filtros-rotulo">Categoria</span>
                        <button type="button" class="btn-filtro ativo" data-filtro="tipo" data-valor="">Todas</button>
                        {{#each filtrosTipo}}
                            <button type="button" class="btn-filtro" data-filtro="tipo" data-valor="{{this.chave}}">
                                <i class="bi {{this.icone}}"></i>{{this.rotuloCurto}}
                            </button>
                        {{/each}}
                    </div>

                    <div class="filtros-grupo mt-2">
                        <span class="filtros-rotulo">Atenção</span>
                        <button type="button" class="btn-filtro ativo" data-filtro="novidade" data-valor="">Todos</button>
                        <button type="button" class="btn-filtro" data-filtro="novidade" data-valor="sim">
                            <i class="bi bi-bell-fill"></i>Com resposta nova
                        </button>
                    </div>

                    <div class="small text-muted mt-3 mb-0">
                        <span data-busca-contador>{{total}}</span> protocolo(s) em exibição.
                        <span class="ms-2 fst-italic" data-busca-status></span>
                    </div>
                </div>
            </div>

            {{#if protocolos.length}}
                <div class="d-flex flex-column gap-3" id="resultados">
                    {{> _linhas_protocolos protocolos=protocolos}}
                </div>
            {{else}}
                <div class="text-center py-5 bg-white rounded-4 shadow-sm">
                    <div class="display-1 text-muted opacity-25 mb-3"><i class="bi bi-folder2-open"></i></div>
                    <h5 class="fw-bold">Você ainda não abriu nenhum protocolo</h5>
                    <p class="text-muted mb-4">
                        Registre uma melhoria ou uma denúncia e acompanhe a resposta da gestão por aqui.
                    </p>
                    <a href="/categories/gestao_de_melhorias/abrir-chamado" class="btn btn-primary rounded-3 px-4 fw-bold me-2">
                        Registrar melhoria
                    </a>
                    <a href="/categories/denuncias_sigilosas/abrir-denuncia" class="btn btn-outline-danger rounded-3 px-4 fw-bold">
                        Registrar denúncia
                    </a>
                </div>
            {{/if}}
        </div>
    </div>
</section>
